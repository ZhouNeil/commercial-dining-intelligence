pandas
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
requests>=2.28.0
# NumPy 1.x vs 2.x pickle BitGenerators differently; merchant *.pkl must match the runtime you use (or retrain).
# If you see "PCG64 is not a known BitGenerator", either: pip install -U "numpy>=2" "scikit-learn>=1.4" OR retrain with NumPy 1.26.
numpy>=1.26.0
scikit-learn>=1.4.0
matplotlib
seaborn
scipy
joblib
xgboost>=2.0.0
folium
sentence-transformers>=2.2.0
